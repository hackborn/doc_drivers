package sqlitegendriver

// autogenerated with github.com/hackborn/onefunc/pipeline on 05 Feb 24 09:22 PST
// do not modify

const (
	genKeysVar        = "$KEYS$"
	genKeyValuesVar   = "$KEYVALUES$"
	genFieldsVar      = "$FIELDS$"
	genTableVar       = "$TABLE$"
	genValuesVar      = "$VALUES$"
	genFieldValuesVar = "$FIELDVALUES$"

	genAndKeyword      = "AND"
	genAndKeywordWS    = " AND "
	genEqualsKeyword   = "="
	genEqualsKeywordWS = " = "

	genQuoteSz = string(rune('\''))

	genSetSql = `INSERT INTO $TABLE$ ($FIELDS$) VALUES($VALUES$) ON CONFLICT($KEYS$) DO UPDATE SET $FIELDVALUES$;`
	genDelSql = `DELETE FROM $TABLE$ WHERE ($KEYVALUES$);`
)

var (
	genDefinitions = map[string]string{
		`Company`: `DROP TABLE IF EXISTS Company;
CREATE TABLE IF NOT EXISTS Company (
	id VARCHAR(255),
	name VARCHAR(255),
	val INTEGER,
	fy INTEGER,
	PRIMARY KEY (id,name)
);`, `Filing`: `DROP TABLE IF EXISTS Filing;
CREATE TABLE IF NOT EXISTS Filing (
	ticker VARCHAR(255),
	end VARCHAR(255),
	form VARCHAR(255),
	val INTEGER,
	units VARCHAR(255),
	fy INTEGER,
	PRIMARY KEY (ticker,end,form)
);`,
	}
)
